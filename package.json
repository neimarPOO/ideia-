{
  "name": "ideia-plus",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "web": "npm run dev --workspace=web",
    "mobile": "npm start --workspace=mobile"
  }
}
